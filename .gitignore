# Build output: the native binary is produced by `ray build --native` and shipped
# through the GitHub Release, never committed.
/bin/raycode
/dist/

# Dependencies fetched by `ray fetch`.
/.ray-deps/

# Leftovers of a native build (`ray build --native` writes them as it goes).
/build.log
/build.status

# macOS
.DS_Store
